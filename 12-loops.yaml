- name: loop
  hosts: localhost
  tasks:
  - name: print persons
    ansible.builtin.debug:
     msg: "Hello {{item}}" # item is a default keyword
    loop:
    - Sai 
    - Anil 
    - naga 
    - hem


- name: secons loop
  hosts: localhost
  tasks:
  - name: print items
    ansible.builtin.debug:
      msg: "print {{item}}"
    loop:
    - apple
    - ball
    - cat 
    - bat










